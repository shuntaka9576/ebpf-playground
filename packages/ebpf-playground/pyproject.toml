[project]
name = "ebpf-playground"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "shuntaka9576", email = "shuntaka9576@gmail.com" }
]
dependencies = []
readme = "README.md"
requires-python = ">= 3.8"

[project.scripts]
hello = "ebpf_playground:hello"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/ebpf_playground"]
